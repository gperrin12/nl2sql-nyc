# Anthropic
ANTHROPIC_API_KEY=sk-ant-...
# Optional override; defaults to claude-sonnet-4-5
CLAUDE_MODEL=claude-sonnet-4-5

# Optional: multi-turn NL→SQL with schema tools (list_tables, get_schema). Higher latency/cost, fewer hallucinated columns.
CLAUDE_SQL_AGENT=false

# Browser: stream tool rounds + guardrails + Athena start over SSE (requires CLAUDE_SQL_AGENT=true on server).
NEXT_PUBLIC_AGENT_SSE=false

# AWS / Athena
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=...
AWS_SECRET_ACCESS_KEY=...
ATHENA_DATABASE=nyc_tlc
ATHENA_OUTPUT_LOCATION=s3://your-bucket/athena-results/
# Optional: workgroup with bytes-scanned cap configured in AWS console
ATHENA_WORKGROUP=primary

# Auth — single shared password for v1
APP_PASSWORD=change-me

# Mapbox (only needed for v2 with map)
NEXT_PUBLIC_MAPBOX_TOKEN=
